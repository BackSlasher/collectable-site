Store collection in browser, initialize if missing
Look at collection
Acquire today's collectable


Today's collectable:
  Load collectables json
  Seed random by today's date (no hours, minutes, seconds)
  Created weighted array (each item multiplied by rarity)
  Random-pick from the array

Collection:
  If local storage doesn't exist, create one
  When collecting item, add it to the local storage
  Schema:
    Category (eggs), Name, image (href), collected on (date)


UI:
  Today's item, "collect" button (grayed out if already collected, check db for items collected today)
  List of items, gallery view
